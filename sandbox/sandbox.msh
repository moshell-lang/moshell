

if { ls } {
    echo ls done successfully
    if [[ -e /tmp/foo ]] {
        echo tmp/foo exists
    } else if ls -l {
        echo ls -l works
    } else {
        echo neither of ls commands options works
    }
} else {
    echo could not list files
}