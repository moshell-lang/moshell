

fun assert(test: Bool) = {
    if !$test
        reef::std::panic("assertion failed")
}

fun assert_msg(test: Bool, msg: String) = {
    if !$test
        reef::std::panic("assertion failed: $msg")
}